apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-config
  namespace: webapp
data:
  DEBUG: "True"  
  ALLOWED_HOSTS: "*"
  USE_POSTGRES: "true"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "webappdb"